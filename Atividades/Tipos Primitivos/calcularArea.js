// Atividade 04

function calcularArea() {
  let base = 9;
  let altura = 7;

  return (base * altura) / 2;
}

const area = calcularArea();
console.log(`A área to triângulo é igual a ${area.toFixed(1)}`);
